const roles = {
  admin: 'admin',
  student: 'student',
  teacher: 'teacher',
}

const permissions = {}
const socketConnections = {
  qrSession: 'qr session',
}

module.exports = { roles,
  permissions,
  socketConnections }
